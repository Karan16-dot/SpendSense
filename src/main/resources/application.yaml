spring:

  datasource:
    url: jdbc:postgresql://localhost:5432/spendsense
    username: postgres
    password: Sandhu@16

  jpa:
    hibernate:
      ddl-auto: update

    show-sql: true

    properties:
      hibernate:
        format_sql: true

  flyway:
    enabled: true
    baseline-on-migrate: true
    locations: classpath:db/migration

springdoc:
  swagger-ui:
    path: /swagger-ui.html

  api-docs:
    path: /v3/api-docs

jwt:
  secret: SpendSenseSecretKeyForJWTAuthentication2026SuperSecure123
  expiration: 86400000